# Environment
.env
.venv/
__pycache__/
*.pyc
.pytest_cache/

# GCP service account keys
*.json

# dbt — profiles.yml is committed (env_var() references only, no literal
# credentials), so it's deliberately not listed here.
dbt_project/target/
dbt_project/dbt_packages/
dbt_project/logs/

# OS
.DS_Store
